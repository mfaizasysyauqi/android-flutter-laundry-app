// File: lib/presentation/style/colors/text_colors.dart
// Berisi definisi warna untuk teks dalam berbagai konteks UI.
// Digunakan untuk memastikan konsistensi warna teks di seluruh aplikasi.

// Mengimpor package dan file yang diperlukan.
import 'package:flutter/material.dart';

// Kelas untuk mendefinisikan warna teks
class TextColors {
  // Warna untuk judul atau heading
  static const Color titleText = Color(0xFF1F2227);
  // Warna untuk teks data utama
  static const Color dataText = Color(0xFF1F2227);
  // Warna untuk teks pendukung seperti petunjuk formulir
  static const Color supportText = Color(0xFF606060);
  // Warna untuk teks pada keadaan kosong
  static const Color emptyStateText = Color(0xFF898B96);
  // Warna untuk teks putih pada sambutan, AppBar, dan tombol
  static const Color lightText = Color(0xFFFFFFFF);
  // Warna untuk teks metadata seperti tanggal
  static const Color metadataText = Color(0xFF757575);
  // Warna untuk teks petunjuk pada formulir
  static const Color formHintText = Color(0xFF9E9E9E);
  // Warna untuk teks tubuh standar
  static const Color bodyText = Color(0xFF1F2227);
  // Warna untuk teks yang disorot
  static const Color highlightedText = Color(0xFF95BBE3);
  // Warna untuk teks hitam murni
  static const Color pureBlackText = Color(0xFF000000);
  // Warna untuk pesan kesalahan
  static const Color errorTextColor = Colors.red;
  // Warna untuk teks aksi pembatalan
  static const Color cancelActionTextColor = Color(0xFFD32F2F);
}